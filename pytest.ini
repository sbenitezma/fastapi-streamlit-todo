[pytest]
# "." for the backend package (api.*), "frontend" for the dashboard package (app.*)
pythonpath = . frontend
testpaths = tests frontend/tests
# coverage settings live in pyproject.toml ([tool.coverage.*]); fail_under is enforced there
addopts = --cov --cov-report=term-missing
