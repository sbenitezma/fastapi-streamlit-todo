# Dev/test dependencies. Installed in the shared Docker image (so `run.ps1 test`
# works) and in CI. Includes everything in requirements.txt.
-r requirements.txt
pytest==9.1.1
httpx==0.28.1
ruff==0.16.6
mypy==2.3.1
